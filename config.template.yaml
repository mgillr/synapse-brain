# Synapse Brain v7 Swarm Configuration
# Copy to config.yaml and fill in your values.
# Minimum: hf_token + at least one LLM provider key.
# Keys stay local -- never committed to git (.gitignore covers config.yaml)

# HuggingFace token (required -- free account works)
# Get one at: https://huggingface.co/settings/tokens
hf_token: ""

# Your HF username or org (Spaces deploy here)
# Leave empty to auto-detect from your token.
hf_owner: ""

# Spore naming prefix (default: synapse-spore)
prefix: "synapse-spore"

# Number of spores (3 for personal use, 7 for full diversity)
count: 3

# Private Spaces (true = only you can access)
private: true

# LLM Provider API Keys
# The swarm tries ALL configured providers with automatic failover.
# More keys = more resilient. At least one is required.
# Provider rotation spreads load across spores to avoid rate limits.
#
# Free signup links:
#   Z.ai:       https://open.bigmodel.cn/       (GLM-4 thinking models, daily limit)
#   xAI Grok:   https://console.x.ai/           (Grok-3-mini, thinking model, fast)
#   OpenRouter: https://openrouter.ai/          (27+ free models, per-model limits)
#   Google AI:  https://aistudio.google.com/    (Gemini 2.5 Flash, 15 req/min)
#   LLM API:    https://llmapi.com/             (Llama-4-Maverick, worker tier)
#
# The swarm uses multiple OpenRouter free models as separate
# rate-limit buckets. One OpenRouter key gives you 5+ providers.
api_keys:
  zai: ""              # Primary brain tier -- GLM thinking models
  xai: ""              # xAI Grok-3-mini brain tier (thinking model)
  openrouter: ""       # 5 free models with independent rate limits
  google_ai: ""        # Reserve -- Gemini 2.5 Flash
  llmapi: ""           # LLM API -- Llama-4-Maverick worker tier

# Command Center (optional)
# deploy_cc: true         # Deploy a CC Space automatically
# cc_name: "synapse-cc"  # CC Space name

# Join an existing swarm (optional)
# Add peer spore URLs to connect to a running swarm.
# Your spores will gossip with these peers, sharing memories and tasks.
# peers:
#   - "https://orgname-synapse-spore-000.hf.space"
#   - "https://orgname-synapse-spore-001.hf.space"

# Model assignments (optional -- defaults to diverse mix)
# models:
#   - "Qwen/Qwen3-235B-A22B"
#   - "meta-llama/Llama-3.3-70B-Instruct"
#   - "deepseek-ai/DeepSeek-R1-Distill-Qwen-32B"

# Advanced: enable Sentinel (autonomous code deployment)
# Requires GITHUB_TOKEN as HF Space secret
sentinel: false

# Advanced: enable Cortex (local micro-LLM on Sentinel)
# Requires ~3GB disk for Qwen3-4B model
cortex: false
