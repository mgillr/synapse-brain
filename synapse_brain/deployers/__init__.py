"""Deployers -- platform-specific deployment scripts.

Each deployer generates the files needed to deploy a spore on a specific
free-tier platform: Dockerfiles, app.py, systemd units, worker scripts.
"""
